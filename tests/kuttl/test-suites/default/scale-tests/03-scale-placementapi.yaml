apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |
      oc patch placementapi -n placement-kuttl-tests placement --type='json' -p='[{"op": "replace", "path": "/spec/replicas", "value":2}]'
